import nodemailer from 'nodemailer'; export default async function handler(req,res){ if(req.method!=='POST') return res.status(405).end(); const {name,phone,email,address,desc,date} = req.body; const transporter = nodemailer.createTransport({ host: process.env.SMTP_HOST, port: Number(process.env.SMTP_PORT || 587), secure: false, auth: { user: process.env.SMTP_USER, pass: process.env.SMTP_PASS } }); const to = process.env.CONTACT_EMAIL || 'rmack@placlaim.com'; const subject = `New lead: ${name} — ${phone}`; const text = `Name: ${name}\nPhone: ${phone}\nEmail: ${email}\nAddress: ${address}\nDate of loss: ${date}\n\nDescription:\n${desc}`; try{ await transporter.sendMail({ from: process.env.SMTP_USER, to, subject, text }); return res.status(200).json({ok:true}) } catch(e){ console.error(e); return res.status(500).json({error:'failed to send'}) } }